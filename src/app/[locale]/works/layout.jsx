export const metadata = {
	title: 'Works | Emil Murahas',
	description: 'Work from the simplest to complex full-stack development.',
};

export default function Works({ children }) {
	return <>{children}</>;
}
